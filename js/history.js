import Auth from './auth.js'; import { escapeHtml, formatDate, formatCurrency } from './utils.js';
const History = { renderHistory(){/* legacy - most history is in audit or reports */}};
export default History;
